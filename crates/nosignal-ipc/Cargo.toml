[package]
name = "nosignal-ipc"
description = "Shared IPC plumbing for NoSignal clients and daemon (DBus on Linux, named pipes on Windows)."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
